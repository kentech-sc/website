# 저장소에는 항상 LF로 저장하고, 체크아웃할 때도 LF를 유지한다.
# (Windows의 core.autocrlf=true가 CRLF로 바꿔버리면 `prettier --check`가 전부 실패한다.)
* text=auto eol=lf

# 바이너리 파일은 변환하지 않는다.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
*.pdf binary
*.xlsx binary
